<h1>編集画面</h1>

<%= form_with model: @training_record, url: muscle_part_path  do |f| %>

  <h4>種目</h4>
  <%= f.text_field :training_event %>

  <h4>メモ</h4>
  <%= f.text_area :training_note %>

  <%= f.submit '保存' %>

<% end %>